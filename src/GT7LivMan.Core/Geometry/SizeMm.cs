namespace GT7LivMan.Core.Geometry;

/// <summary>Physical plate/canvas size in millimeters — the unit every template and the exported viewBox use.</summary>
public readonly record struct SizeMm(double Width, double Height);
